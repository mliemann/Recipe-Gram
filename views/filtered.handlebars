            <a
              href="#"
              class="nav-link filter-modal"
              data-toggle="modal"
              data-target=".bd-example-modal-lg"
            >Filter Results</a>

            <div
              class="modal fade bd-example-modal-lg filter-modal"
              tabindex="-1"
              role="dialog"
              aria-labelledby="myLargeModalLabel"
              aria-hidden="true">
              <div class="modal-dialog modal-lg">
                <div class="modal-content">
                        <div class="form-group">
        <fieldset class="large-5 cell filter-category">
          <legend>Select a Category:</legend>
          <input
            type="checkbox"
            name="filter"
            value="1"
            class= "filtered"
          /><label class="mr-1" for="breakfast">Breakfast</label>
          <input
            type="checkbox"
            name="filter"
            value="2"
            class= "filtered"
          /><label class="mr-1" for="lunch">Lunch</label>
          <input
            type="checkbox"
            name="filter"
            value="3"
            class= "filtered"
          /><label class="mr-1" for="dinner">Dinner</label>
          <input
            type="checkbox"
            name="filter"
            value="4"
            class= "filtered"
          /><label class="mr-1" for="dessert">Dessert</label>
          <input
            type="checkbox"
            name="filter"
            value="5"
            class= "filtered"
          /><label class="mr-1" for="snack">Snack</label>
            <button class="btn btn-primary filterBtn">Find</button>
        </fieldset>
      </div>

                </div>
              </div>
            </div>


{{#each filters as |filter| }}

  <div class="card mb-3" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src={{filter.url}} class="img-fluid rounded-start" alt="recipe image">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title"><a href="/recipe/{{filter.id}}">{{filter.recipe_name}}</a></h5>
        <p class="card-text">{{filter.description}}</p>
        <p class="card-text"><small class="text-muted">Uploaded by {{filter.user.user_name}}</small></p>
      </div>
    </div>
  </div>
</div>
</div>{{/each}}

